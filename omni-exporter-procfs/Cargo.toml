[package]
name = "omni-exporter-procfs"
version = "0.1.0"
edition = "2024"

[dependencies]
log = { version = "0.4" }
tokio = { version = "1", features = ["rt", "macros", "sync", "fs", "io-util"] }

[lints]
workspace = true
