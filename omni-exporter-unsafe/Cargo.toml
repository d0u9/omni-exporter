[package]
name = "omni-exporter-unsafe"
version = "0.1.0"
edition = "2024"
build = "build/main.rs"

[dependencies]

[target.'cfg(target_os = "macos")'.dependencies]
libc = "0.2"

[build-dependencies]
bindgen = "*"

[lints]
workspace = true
